<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Trámites de Bautismo</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

  <nav class="navbar navbar-dark bg-dark">
    <div class="container">
      <a class="navbar-brand" href="/">Parroquia &mdash; Bautismos</a>
    </div>
  </nav>

  <div class="container py-4">
    <h2>Trámites de Bautismo</h2>
    <div class="mb-3">
      <input type="text" class="form-control" placeholder="Buscar por nombre del bautizado...">
    </div>
    <table class="table">
      <thead>
        <tr>
          <th>ID</th>
          <th>Bautizado</th>
          <th>Fecha ingreso</th>
          <th>Estado</th>
          <th>Fecha bautismo</th>
        </tr>
      </thead>
      <tbody>
        <% tramites.forEach(function(item){ %>
          <% const bautizado = item.Participacions && item.Participacions[0] ? item.Participacions[0].Persona : null; %>
          <tr>
              <td><%= item.id %></td>
              <td><%= bautizado ? bautizado.nombre + ' ' + bautizado.apellido : '—' %></td>
              <td><%= item.fecha_ingreso %></td>
              <td><%= item.estado %></td>
              <td><%= item.fecha_bautismo %></td>
          </tr>
       <% }); %>

      </tbody>
    </table>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/js/lista.js"></script>
</body>
</html>
