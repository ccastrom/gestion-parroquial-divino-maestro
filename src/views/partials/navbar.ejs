<nav class="navbar navbar-dark bg-dark">
  <div class="container">
    <a class="navbar-brand" href="/web">Parroquia &mdash; Bautismos</a>
    <div class="d-flex gap-2">
      <a href="/web"             class="btn btn-sm <%= paginaActual === 'tramites'   ? 'btn-light' : 'btn-outline-light' %>">Trámites</a>
      <a href="/web/calendario"  class="btn btn-sm <%= paginaActual === 'calendario' ? 'btn-light' : 'btn-outline-light' %>">Calendario</a>
      <a href="/web/personas"    class="btn btn-sm <%= paginaActual === 'personas'   ? 'btn-light' : 'btn-outline-light' %>">Personas</a>
      <form action="/logout" method="POST" class="m-0">
        <button type="submit" class="btn btn-sm btn-outline-light">Cerrar sesión</button>
      </form>
    </div>
  </div>
</nav>
